namespace EventTriggerSystem
{
    public class EventActionManager : MonoBehaviourSingleton<EventActionManager>
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
